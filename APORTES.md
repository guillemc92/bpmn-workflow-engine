# Aportes individuales

> Plantilla a completar por el equipo. En esta entrega el motor se construyo
> en solitario (ver `docs/prompt_mappings.md`); las filas quedan vacias para
> que cada integrante registre su contribucion real antes de la entrega final.

| Integrante | Componente(s) | Contribucion | % aproximado |
|---|---|---|---|
| | Domain model (`domain/`) | | |
| | Runtime / instancia (`runtime/`) | | |
| | Compuertas logicas (`domain/gates.py`) | | |
| | Persistencia (`persistence/`) | | |
| | Orquestacion (`orchestration/`) | | |
| | Incidentes / reset / reintentos | | |
| | SLA / concurrencia (`execution/`) | | |
| | Tests de escenarios (`tests/scenarios/`) | | |
| | Documentacion (PRD, FSD, prompt mappings) | | |

## Notas

- Completar con nombre, componente(s) tocados, una frase de que se hizo, y
  el porcentaje aproximado de esfuerzo dedicado por esa persona a ese
  componente (no tiene que sumar 100% por fila si hubo pair-programming).
- Referenciar el/los commit(s) de cada integrante donde sea posible (`git log --author`).
